import { PageShell } from "@/components/shared/page-shell";

export default function AupPage() {
  return (
    <PageShell maxWidth="md" className="py-12">
      <h1 className="text-2xl font-bold text-slate-900">Acceptable Use Policy</h1>
      <p className="mt-4 text-sm text-slate-600">
        Draft placeholder — Phase 0. Lawyer-reviewed text from legal/05-ACCEPTABLE-USE-POLICY.txt will replace this before public launch (Phase 3).
      </p>
    </PageShell>
  );
}