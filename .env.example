# =============================================================================
# ALINKS local dev — NO DOCKER REQUIRED
# See LOCAL_DEV_SETUP.txt for Neon (recommended), Homebrew, or Supabase setup.
# =============================================================================

# Option A — Neon (recommended, zero local RAM):
# DATABASE_URL=postgresql://user:pass@ep-xxx.region.aws.neon.tech/alinks_platform?sslmode=require

# Option B — Homebrew Postgres on Mac:
# DATABASE_URL=postgresql://alinks_user@localhost:5432/alinks_platform

# Option C — Supabase platform project (direct connection):
# DATABASE_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres

DATABASE_URL=

# Superadmin phone (OTP login grants superadmin role — last 10 digits matched)
SUPERADMIN_PHONE=9999999999

# Dev OTP bypass (never use in production)
DEV_OTP=1111

# App URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_PLATFORM_HOST=app.localhost:3000
NEXT_PUBLIC_MARKETING_HOST=localhost:3000
NEXT_PUBLIC_ROOT_DOMAIN=localhost:3000

# Redis — Upstash cloud only, Phase 2+ (not needed for Phase 0)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=